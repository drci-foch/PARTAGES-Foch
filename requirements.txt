pyodbc>=4.0.39
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
python-dotenv>=1.0.0
pdfplumber>=0.10.0

# NB : pas d'OCR pour CU5a/CU5b — les documents à océriser (scans) sont écartés faute de méthode
# d'OCR fiable ; on ne conserve que la couche texte native. Aucune dépendance OCR requise.
